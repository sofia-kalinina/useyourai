Review and update Claude-facing context files to reflect the current state of the project:

1. CLAUDE.md — update stack, architecture, rules, or commands if anything has shifted
2. docs/ — update if scenarios or plans have evolved or been completed
3. .claude/commands/*.md — update command files if patterns or conventions changed

Only change what's actually different. Do not rewrite for style.
After updating, summarize what you changed and why.
